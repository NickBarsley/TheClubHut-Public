%div{ :class => "kitlocker" }
  - @photo = Photo.find(:first, :conditions => ["item_id=?", kitlistitem.id])
  - if @photo
    .photo
      %TABLE{:style => "border: 0; cell-padding: 0; cell-spacing: 0;"}
        %TR
          %TD
            = link_to image_tag(@photo.public_filename('tiny'), :class => "photo_thumb", :title => [kitlistitem.name + " Manufacturers Photos."]), item_path(kitlistitem.id)
          %TD{:style => "v-align: center; white-space: nowrap;"}
            = link_to kitlistitem.name, item_path(kitlistitem.id)
            = "- " + pluralize(kitlistitem.review.size, "review")
  - else
    .photo.smaller.center
      .title
        = link_to kitlistitem.name, item_path(kitlistitem.id)
        = "- " + pluralize(kitlistitem.review.size, "review")
      [
      = link_to "Add a Photo", new_item_photo_path(kitlistitem.id)
      ]
  %div{:style => "clear: both; height: 0"}
  %BR